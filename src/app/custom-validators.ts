import { FormControl } from '@angular/forms';

// Custo validators to be built

// export function validateEmail(c: FormControl) {
//   let EMAIL_REGEXP = '^([a-zA-Z0-9_\-\.]+)@([a-zA-Z0-9_\-\.]+)\.([a-zA-Z]{2,5})$'

//   return EMAIL_REGEXP.test(c.value) ? null : {
//     validateEmail: {
//       valid: false
//     }
//   };
// }